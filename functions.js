/*function decideAction(input){
    if (input == "start") {
        return "starting the process";
        //console.log("starting the process");
    } else if (input == "stop") {
        return "stopping the process";
        //console.log("stopping the process");
    } else {
        return "unknown action";
        //console.log("unknown action");
    }
}

x = decideAction("start");
y = decideAction("stop");
z = decideAction("pause");

console.log(x);
console.log(y);
console.log(z);

function studentMarks(marks) {
  if (marks >= 75) {
    return "Grade A";
  } else if (marks >= 65) {
    return "Grade B";
  } else if (marks >= 55) {
    return "Grade C";
  } else if (marks >= 35) {
    return "Grade S";
  } else {
    return "Grade F";
  }
}

stu1 = studentMarks(85);
stu2 = studentMarks(48);
stu3 = studentMarks(25);

console.log(stu1);
console.log(stu2);
console.log(stu3);*/

/*const decideActionArrow = (input) => {
  switch (input) {
    case "start":
      return "starting the process";
    case "stop":
      return "stopping the process";
    default:
      return "unknown action";
  }
};

decideActionArrow("start");
decideActionArrow("stop");
decideActionArrow("pause");*/


const addValues = (num1, num2) => {
    sum = num1 + num2;
    return sum;
}

Sum = addValues(10, 20);
console.log(Sum);


